import React from 'react';
import styles from  './styles.module.css';

export const Header = (props) => {
    return (
        <h1 className={styles.headerPage}>Header</h1>
    )
}
export default Header;