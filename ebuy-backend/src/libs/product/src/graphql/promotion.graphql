type ProductSale implements Node {
    _id: ID!
    discount: Int!
    percentage: Int!
    createdAt: Date
    updatedAt: Date
}
input ProductSaleConfig {
    discount: Int!
    percentage: Int!
}
type Mutation {
    setProductPromotion(id: ID!, config: ProductSaleConfig!): Product!
}